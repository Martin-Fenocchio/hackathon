<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
            background-color: #f2f0ff;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }
        .voucher-card {
            background: white;
            width: 720px;
            height: 420px;
            border-radius: 20px;
            padding: 40px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            position: relative;
        }
        .title {
            color: #888;
            font-size: 20px;
            text-align: center;
            margin-bottom: 20px;
        }
        .amount {
            color: #000;
            font-size: 36px;
            font-weight: bold;
            text-align: center;
            margin-bottom: 20px;
        }
        .status {
            color: #008000;
            font-size: 20px;
            text-align: center;
            margin-bottom: 40px;
        }
        .details {
            color: #444;
            font-size: 18px;
            line-height: 35px;
        }
        .border-decoration {
            position: absolute;
            top: 20px;
            left: 20px;
            right: 20px;
            bottom: 20px;
            border: 2px solid #ddd;
            border-radius: 10px;
            pointer-events: none;
        }
    </style>
</head>
<body>
    <div class="voucher-card">
        <div class="border-decoration"></div>
        <div class="title">Transferencia realizada</div>
        <div class="amount">$ {{amount}}</div>
        <div class="status">✓ Transferencia enviada</div>
        <div class="details">
            <div>ID de Transferencia: {{transferid}}</div>
            <div>Clave Pública Destino:</div>
            <div>{{destination_publickey}}</div>
            <div>Fecha: {{date}}</div>
        </div>
    </div>
</body>
</html> 